const config = {
  mockedData: false, // true: fetch-mock, false: API
  siteTitle: 'Product Catalogue',
  siteUrl: 'http://www.add_production_url_here.com',
  siteShortName: 'product-catalogue',
  offlineFirst: false, // TODO: Check if needed
};

module.exports = config;
